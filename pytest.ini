[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
asyncio_mode = auto

# Ignore warnings
filterwarnings =
    ignore::DeprecationWarning

# Coverage options
addopts = 
    --verbose
    --strict-markers
    -ra
